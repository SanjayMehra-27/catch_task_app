import 'package:flutter/material.dart';

class MyPinsTasksWidget extends StatelessWidget {
  const MyPinsTasksWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('My Pins Task'),
      ),
    );
  }
}